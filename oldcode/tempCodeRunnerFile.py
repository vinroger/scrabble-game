s = self.question
        # lst = list(s.split(' '))
        # random.shuffle(lst)
        # self.question_list = lst
        # s = ''.join(lst)
        # question = s

        # for i in range(len(question)):
        #     self.button_list.append(tk.Button(self.answer_buttons_frame),
        #                             text=question[i].upper(), command=lambda idx=i: self.assign_letter(question[idx].upper(), self.button_list[idx]))
        #     self.button_list[i].grid(row=1, column=i, padx=5, pady=5)